# Chezmoi configuration file
# Documentation: https://www.chezmoi.io/reference/configuration-file/

[git]
    autoCommit = false
    autoPush = false  # Good to be explicit about not auto-pushing secrets

[onepassword]
    mode = "service"  # Use service account token (OP_SERVICE_ACCOUNT_TOKEN)
    prompt = false    # Important for automation
